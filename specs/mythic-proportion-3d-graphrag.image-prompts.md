# Image prompts - mythic-proportion-3d-graphrag

Generate each image at the path shown, then run:  `plan-images.ps1 apply mythic-proportion-3d-graphrag.html`
Any slot left ungenerated stays a graceful "image pending" placeholder - the plan is fully usable without it.

Universal style spec (already appended to every prompt below):
> 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.

---

## hero  →  `mythic-proportion-3d-graphrag/hero.png`  [PENDING]
**Prompt (copy-paste):**
```
a luminous 3D knowledge-graph constellation of glowing note-nodes clustered into colored communities, a calm reading pane docked to one side, rendered as one clear "second brain you can fly through" metaphor. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a luminous 3D knowledge-graph constellation of glowing note-nodes clustered into colored communities, a calm reading pane docked to one side, rendered as one clear "second brain you can fly through" metaphor

## problem  →  `mythic-proportion-3d-graphrag/problem.png`  [PENDING]
**Prompt (copy-paste):**
```
labelled boxes-and-arrows of the CURRENT architecture: Markdown vault -> SQLite (pages/fts/vectors only) -> FastAPI -> vanilla-JS SPA with a flat 2D canvas graph; a cloud LLM box with note text flowing to it unredacted; big gaps drawn where entities/claims/communities/agents/MCP/3D should be. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** labelled boxes-and-arrows of the CURRENT architecture: Markdown vault -> SQLite (pages/fts/vectors only) -> FastAPI -> vanilla-JS SPA with a flat 2D canvas graph; a cloud LLM box with note text flowing to it unredacted; big gaps drawn where entities/claims/communities/agents/MCP/3D should be

## solution  →  `mythic-proportion-3d-graphrag/solution.png`  [PENDING]
**Prompt (copy-paste):**
```
target architecture visually parallel to the problem diagram: same Markdown vault -> Python core (ingest/compile/index/query) -> SQLite now holding entities/relationships/claims/communities/reports + vec0 tables; a privacy-redactor box between the vault and BOTH a cloud LLM and a local Ollama LLM; a retrieval engine (global/local/DRIFT/spreading-activation); a Vite+R3F 3D graph frontend; an agents box (Extractor/Refiner/Librarian); an MCP server exposed to an external coding agent. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** target architecture visually parallel to the problem diagram: same Markdown vault -> Python core (ingest/compile/index/query) -> SQLite now holding entities/relationships/claims/communities/reports + vec0 tables; a privacy-redactor box between the vault and BOTH a cloud LLM and a local Ollama LLM; a retrieval engine (global/local/DRIFT/spreading-activation); a Vite+R3F 3D graph frontend; an agents box (Extractor/Refiner/Librarian); an MCP server exposed to an external coding agent

## phase0  →  `mythic-proportion-3d-graphrag/phase0.png`  [PENDING]
**Prompt (copy-paste):**
```
two side-by-side repository icons (mythic-proportion and FABLE-HARNESS) each with its own main branch and a public badge, a dotted "sibling link" arrow between them, and a feat/3d-graphrag branch sprouting from mythic-proportion. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** two side-by-side repository icons (mythic-proportion and FABLE-HARNESS) each with its own main branch and a public badge, a dotted "sibling link" arrow between them, and a feat/3d-graphrag branch sprouting from mythic-proportion

## phase1  →  `mythic-proportion-3d-graphrag/phase1.png`  [PENDING]
**Prompt (copy-paste):**
```
a design-system board: an OKLCH color ramp, a type scale, spacing/radius chips, a Cmd+K palette mockup, and a small arrow showing a --graph-community token flowing into a glowing 3D node. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a design-system board: an OKLCH color ramp, a type scale, spacing/radius chips, a Cmd+K palette mockup, and a small arrow showing a --graph-community token flowing into a glowing 3D node

## phase2  →  `mythic-proportion-3d-graphrag/phase2.png`  [PENDING]
**Prompt (copy-paste):**
```
a before/after parity scale: the legacy vanilla-JS app on one pan and the new React app on the other, balanced level, with a checklist of CLI verbs and web routes all ticked between them. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a before/after parity scale: the legacy vanilla-JS app on one pan and the new React app on the other, balanced level, with a checklist of CLI verbs and web routes all ticked between them

## phase3  →  `mythic-proportion-3d-graphrag/phase3.png`  [PENDING]
**Prompt (copy-paste):**
```
a markdown page fanning out into a small labelled graph of entity nodes and typed relationship edges plus claim cards, with a cache token showing "hit" on unchanged chunks. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a markdown page fanning out into a small labelled graph of entity nodes and typed relationship edges plus claim cards, with a cache token showing "hit" on unchanged chunks

## phase4  →  `mythic-proportion-3d-graphrag/phase4.png`  [PENDING]
**Prompt (copy-paste):**
```
four small panels labelled Global/Local/DRIFT/Spreading-activation, each showing the same graph lit up differently — communities as hulls for global, a neighborhood expansion for local, a primer+follow-ups tree for DRIFT, a heat-spreading BFS for activation. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** four small panels labelled Global/Local/DRIFT/Spreading-activation, each showing the same graph lit up differently — communities as hulls for global, a neighborhood expansion for local, a primer+follow-ups tree for DRIFT, a heat-spreading BFS for activation

## phase5  →  `mythic-proportion-3d-graphrag/phase5.png`  [PENDING]
**Prompt (copy-paste):**
```
a dense but legible 3D force graph of ~50k glowing points grouped into colored community hulls, one node hover-highlighted with its neighbors lit and the rest dimmed, a reading pane docked at right, an FPS meter reading 60. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a dense but legible 3D force graph of ~50k glowing points grouped into colored community hulls, one node hover-highlighted with its neighbors lit and the rest dimmed, a reading pane docked at right, an FPS meter reading 60

## phase6  →  `mythic-proportion-3d-graphrag/phase6.png`  [PENDING]
**Prompt (copy-paste):**
```
a note passing through a local "redactor" filter that blacks out PII tokens, then forking to either a cloud LLM or a local Ollama LLM, then a rehydrator restoring the PII into the answer; embeddings shown staying on-device. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a note passing through a local "redactor" filter that blacks out PII tokens, then forking to either a cloud LLM or a local Ollama LLM, then a rehydrator restoring the PII into the answer; embeddings shown staying on-device

## phase7  →  `mythic-proportion-3d-graphrag/phase7.png`  [PENDING]
**Prompt (copy-paste):**
```
three labelled agent figures — Extractor feeding the graph, Refiner tidying/merging duplicate nodes with a human-confirm gate, Librarian assembling a ranked context bundle — around the central knowledge graph. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** three labelled agent figures — Extractor feeding the graph, Refiner tidying/merging duplicate nodes with a human-confirm gate, Librarian assembling a ranked context bundle — around the central knowledge graph

## phase8  →  `mythic-proportion-3d-graphrag/phase8.png`  [PENDING]
**Prompt (copy-paste):**
```
an external coding-agent icon connecting over an MCP stdio pipe to the mythic brain, pulling context (read) and pushing a gated, audited write, with a read-only padlock and a JSONL audit scroll. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** an external coding-agent icon connecting over an MCP stdio pipe to the mythic brain, pulling context (read) and pushing a gated, audited write, with a read-only padlock and a JSONL audit scroll

## phase9  →  `mythic-proportion-3d-graphrag/phase9.png`  [PENDING]
**Prompt (copy-paste):**
```
a local ComfyUI node-graph on one side feeding transparent glyph/texture PNGs into the mythic design-assets folder and onto glowing graph nodes on the other side, a "localhost only, no cloud" badge. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a local ComfyUI node-graph on one side feeding transparent glyph/texture PNGs into the mythic design-assets folder and onto glowing graph nodes on the other side, a "localhost only, no cloud" badge

## phase10  →  `mythic-proportion-3d-graphrag/phase10.png`  [PENDING]
**Prompt (copy-paste):**
```
the feat/3d-graphrag branch merging into main with a version tag, two public repos pushed (green check badges), and the legacy 2D UI fading out as the 3D app takes over. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** the feat/3d-graphrag branch merging into main with a version tag, two public repos pushed (green check badges), and the legacy 2D UI fading out as the 3D app takes over

## notes  →  `mythic-proportion-3d-graphrag/notes.png`  [PENDING]
**Prompt (copy-paste):**
```
a phase dependency matrix / DAG: P0 scaffold -> P1 design-system and P2 parity; P2 -> P3 data -> P4 retrieval -> P5 3D; P2 -> P6 privacy -> P7 agents; everything -> P8 MCP; P9 ComfyUI off to the side; all converging on P10 cutover. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a phase dependency matrix / DAG: P0 scaffold -> P1 design-system and P2 parity; P2 -> P3 data -> P4 retrieval -> P5 3D; P2 -> P6 privacy -> P7 agents; everything -> P8 MCP; P9 ComfyUI off to the side; all converging on P10 cutover

