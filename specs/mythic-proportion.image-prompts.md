# Image prompts — mythic-proportion

Generate each image at the path shown, then run:  `plan-images.ps1 apply mythic-proportion.html`
Any slot left ungenerated stays a graceful "image pending" placeholder — the plan is fully usable without it.

Universal style spec (already appended to every prompt below):
> 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.

---

## hero  →  `mythic-proportion/hero.png`  [PENDING]
**Prompt (copy-paste):**
```
a single funnel where mixed documents images and artifacts drop in at the top and emerge at the bottom as a glowing interconnected constellation of linked wiki note cards, the drop-folder-to-knowledge-graph metaphor. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a single funnel where mixed documents images and artifacts drop in at the top and emerge at the bottom as a glowing interconnected constellation of linked wiki note cards, the drop-folder-to-knowledge-graph metaphor

## problem  →  `mythic-proportion/problem.png`  [PENDING]
**Prompt (copy-paste):**
```
scattered disconnected islands of documents labelled fable-artifacts, memory-decisions, specs, user-PDFs, screenshots, drifting apart with broken dashed lines and question-mark gaps between them, cold muted palette. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** scattered disconnected islands of documents labelled fable-artifacts, memory-decisions, specs, user-PDFs, screenshots, drifting apart with broken dashed lines and question-mark gaps between them, cold muted palette

## solution  →  `mythic-proportion/solution.png`  [PENDING]
**Prompt (copy-paste):**
```
a clean three-layer stacked architecture diagram, bottom layer raw immutable documents, middle layer a dense web of linked markdown wiki pages in blue green and purple nodes, top layer labelled schema index hot-cache and a sqlite hybrid-search chip, a drop-folder arrow feeding the bottom and a query arrow reading from the top, warm confident palette. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a clean three-layer stacked architecture diagram, bottom layer raw immutable documents, middle layer a dense web of linked markdown wiki pages in blue green and purple nodes, top layer labelled schema index hot-cache and a sqlite hybrid-search chip, a drop-folder arrow feeding the bottom and a query arrow reading from the top, warm confident palette

## phase1  →  `mythic-proportion/phase1.png`  [PENDING]
**Prompt (copy-paste):**
```
a clean file-tree diagram of the vault folder showing drop raw wiki sources entities concepts sessions index and vault-meta with schema index and hot markdown files highlighted, plus a terminal chip showing mythic init. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a clean file-tree diagram of the vault folder showing drop raw wiki sources entities concepts sessions index and vault-meta with schema index and hot markdown files highlighted, plus a terminal chip showing mythic init

## phase2  →  `mythic-proportion/phase2.png`  [PENDING]
**Prompt (copy-paste):**
```
a horizontal pipeline diagram, a mixed stack of files entering a router that forks into docling and markitdown parsers, then a dedup hash gate, then splitting into an immutable raw archive box and a parsed-markdown staging box. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a horizontal pipeline diagram, a mixed stack of files entering a router that forks into docling and markitdown parsers, then a dedup hash gate, then splitting into an immutable raw archive box and a parsed-markdown staging box

## phase3  →  `mythic-proportion/phase3.png`  [PENDING]
**Prompt (copy-paste):**
```
a parsed source document on the left feeding into a claude compile engine in the centre, emitting on the right a cluster of linked note cards colour-coded source-green entity-purple concept-blue with wikilink threads between them and a small contradiction flag on one link. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a parsed source document on the left feeding into a claude compile engine in the centre, emitting on the right a cluster of linked note cards colour-coded source-green entity-purple concept-blue with wikilink threads between them and a small contradiction flag on one link

## phase4  →  `mythic-proportion/phase4.png`  [PENDING]
**Prompt (copy-paste):**
```
a sqlite database cylinder split into two labelled lanes FTS5-BM25 and sqlite-vec-cosine, a query arrow entering and fanning across both lanes then merging into a ranked result list, with a small delta icon showing only-changed-pages incremental sync. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a sqlite database cylinder split into two labelled lanes FTS5-BM25 and sqlite-vec-cosine, a query arrow entering and fanning across both lanes then merging into a ranked result list, with a small delta icon showing only-changed-pages incremental sync

## phase5  →  `mythic-proportion/phase5.png`  [PENDING]
**Prompt (copy-paste):**
```
split panel, left side a query flowing through hot-cache then index then pages then a claude answer bubble with citation chips, right side a lint health dashboard listing orphans broken-links and stale-index counts with green checkmarks after fix. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** split panel, left side a query flowing through hot-cache then index then pages then a claude answer bubble with citation chips, right side a lint health dashboard listing orphans broken-links and stale-index counts with green checkmarks after fix

## phase6  →  `mythic-proportion/phase6.png`  [PENDING]
**Prompt (copy-paste):**
```
an eye-shaped watchdog icon hovering over the drop folder with a real-time pulse line, next to an obsidian graph-view screenshot-style constellation of coloured nodes, conveying always-on optional watching plus native obsidian compatibility. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** an eye-shaped watchdog icon hovering over the drop folder with a real-time pulse line, next to an obsidian graph-view screenshot-style constellation of coloured nodes, conveying always-on optional watching plus native obsidian compatibility

## notes  →  `mythic-proportion/notes.png`  [PENDING]
**Prompt (copy-paste):**
```
a capability-vs-dependencies matrix, three columns python-only then plus-local-embeddings then plus-claude-api, rows for ingest search compile query showing progressively more features lighting up, illustrating graceful degradation tiers. 1536x1024, high quality, minimal professional engineering-diagram style, clean flat vector look, generous whitespace, single accent color, <10 words of embedded text, 1-2 core concepts, legible to a software engineer.
```
**alt / caption:** a capability-vs-dependencies matrix, three columns python-only then plus-local-embeddings then plus-claude-api, rows for ingest search compile query showing progressively more features lighting up, illustrating graceful degradation tiers

